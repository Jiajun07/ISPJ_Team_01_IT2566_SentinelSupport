When adding any new platform of implementations, Add to this page
1. Python language through extension
2. pip install flask sqlalchemy psycopg2-binary
3. pip install bcrypt
4. pip install psycopg2
5. pip install wtforms
6. pip install flask_wtf
7. (SHA256 hashing uses Python stdlib hashlib - no extra pip package required)
8. 
py -m pip install flask, sqlalchemy, psycopg2-binary, bcrypt, psycopg2, flask_wtf, wtforms